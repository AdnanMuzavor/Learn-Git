#include <iostream>>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << "mul of " << a << " * " << b << " = " << a * b << endl;
    cin >> a >> b;
    cout << "changes changes mul with new values is: " << a << " * " << b << " = " << a * b << endl;
    return 0;
}
#include <iostream>>
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    cout << "sum of " << a << " + " << b << " = " << a + b << endl;
    cin >> a >> b;
    cout << " sum with new values is: " << a << " + " << b << " = " << a + b << endl;
    return 0;
}